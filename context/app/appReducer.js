import {
  SHOW_ALERT,
  CLEAN_ALERT,
  UPLOAD_FILE_ERROR,
  UPLOAD_FILE_SUCCES,
  CREATE_LINK_ERROR,
  CREATE_LINK_SUCCESS,
} from "../../types";

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
